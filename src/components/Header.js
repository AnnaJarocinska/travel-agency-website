import React from 'react';
import Slider from './Slider';

const Header = () => {
    return (
        <React.Fragment>
            <Slider> </Slider>
        </React.Fragment>
    );
}

export default Header;